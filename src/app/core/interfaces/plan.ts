export interface Plan {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  nom_comida: string;
  descripcion: string;
  energia: string;
  grasa: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  h_carbono: string;
  proteina: string;
  fibra: string;
  tipo: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  lista_alimentos: string;
  horario: string;
}

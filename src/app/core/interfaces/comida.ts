export interface  Comida {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  nom_comida: string;
  descripcion: string;
  energia: string;
  grasa: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  h_carbono: string;
  proteina: string;
  fibra: string;
  tipo: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  estado_comida: boolean;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  lista_alimentos: string;
  porcentaje: number ;
  horario: string;
}
